---
apiVersion: v1
kind: ConfigMap
metadata:
  name: automatch-bff-configmap
  namespace: automatch
data:
  NODE_ENV: "qa"
  PORT: "3020"
  INVENTORY_API_URL: "http://automatch-inventory-bs-svc/v1"
  REDIS_URL: "rediss://:6357ed466025411d8cceb0d5d6b6c786@us1-perfect-hyena-42231.upstash.io:42231"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: automatch-bff-deploy
  namespace: automatch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-automatch-bff
  template:
    metadata:
      labels:
        app: ms-automatch-bff
    spec:
      automountServiceAccountToken: false
      containers:
        - name: ms-automatch-bff
          image: victor0141/ms-automatch-bff:1.3
          imagePullPolicy: Always
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: automatch-bff-configmap
                  key: NODE_ENV
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: automatch-bff-configmap
                  key: PORT
            - name: INVENTORY_API_URL
              valueFrom:
                configMapKeyRef:
                  name: automatch-bff-configmap
                  key: INVENTORY_API_URL
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: automatch-bff-configmap
                  key: REDIS_URL
          ports:
            - containerPort: 3020
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: automatch-bff-svc
  namespace: automatch
spec:
  selector:
    app: ms-automatch-bff
  ports:
    - port: 80
      targetPort: 3020
      protocol: TCP
  type: ClusterIP #[ClusterIP]Expose node for internal k8 comunication, [LoadBalancer]Expose node for external comunication

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-control
  namespace: automatch
spec:
  ingressClassName: nginx
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "automatch-bff-svc"
                port:
                  number: 80

  

